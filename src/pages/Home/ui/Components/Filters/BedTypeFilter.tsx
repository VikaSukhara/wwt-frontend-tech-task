import FilterRendereComponent from './FilterRenderer'

const BedTypeFilter = () => {
	return (
		<FilterRendereComponent
			filterId="BED_TYPE"
			filterKey="filterItems.bedType"
		/>
	)
}

export default BedTypeFilter
